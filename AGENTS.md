# AGENTS.md

本文件定义 AI 编码代理在本仓库中的工作方式。


## 工作规则

- 只用中文沟通，编写文档。
- 修改前先阅读相关代码。
- 优先沿用项目已有模式，不轻易引入新抽象。
- 改动范围应紧贴当前任务或缺陷。
- 不要回滚用户或其他代理的无关改动。
- 除非明确要求，不要提交 `frontend/dist` 等构建产物。
- Flyway 迁移只能追加，不能重写已经应用过的迁移。
- 前端新增接口时，先更新 `frontend/src/api` 中的类型和 API 客户端，再接页面。
- 后端控制器保持轻薄，校验和业务规则放到 service 层。
- 涉及安全的改动，应补服务测试或集成测试后再宣称完成。
- 禁用lombok


## 验证要求

根据改动范围运行最小但有效的验证。

仅后端改动：

```bash
cd backend
./mvnw test -Dtest=<RelevantTest>
```

仅前端改动：

```bash
cd frontend
npm run build
```

跨前后端改动通常应运行：

```bash
cd backend
./mvnw test

cd ../frontend
npm run build
```

如果完整测试成本过高，运行聚焦测试，并说明剩余风险。

## 数据库规则

- 数据库结构变更必须新增 Flyway 迁移，位置为 `backend/src/main/resources/db/migration`。
- 迁移必须保留现有本地和生产数据。
- 对重要不变量使用明确约束。
- 除非是必要种子数据，不要在迁移中插入演示数据。
- 创建动态资产表时，必须遵循现有安全 SQL 命名和元数据映射机制。

## API 规则

- 响应结构保持与 `ApiResponse` 一致。
- 业务错误通过已有 `BusinessException` 和 `ErrorCode` 返回。
- 不要泄露密钥、密码哈希、Token 哈希或内部堆栈。
- 新增前端接口时，在 `frontend/src/api` 中新增或更新 TypeScript 类型。

## 前端规则

- 构建真实可用的应用页面，不做营销页。
- 运营型页面应信息密集、可读、面向工作流。
- 一旦后端接口存在，就不要使用本地 mock fallback；接口失败应明确显示错误。
- 优先复用现有布局、设计变量、卡片、按钮和 Element Plus 模式。
- 表格操作必须清晰，高风险变更需要确认。
- 确保窄屏下文字不溢出，表格必要时使用横向滚动。

## 安全规则

- 登录认证、RBAC、资产权限、访问 Token 和审计日志都属于高风险区域。
- 授权必须在后端 service 或 filter 中执行，不能只依赖 UI。
- 密码变更必须使用配置的 `PasswordEncoder` 哈希。
- 访问 Token 明文只能在创建时显示一次。
- 会导致操作者被锁出系统的变更，最后管理员或最后系统访问检查必须具备事务安全性。

## 卡帕西式代理原则

在本仓库工作时默认遵循以下原则。

1. 简单优先。
   - 优先选择最小且正确的实现。
   - 除非本地代码已经确实需要，不要引入框架、模式、通用引擎或宽泛抽象。

2. 让系统可观察。
   - 调试时检查真实状态：数据库行、HTTP 响应、日志、渲染后的 UI 和测试输出。
   - 如果一个低成本运行时检查能回答问题，就不要只靠读代码猜。

3. 小步迭代。
   - 做一个连贯的小改动，运行一次聚焦验证，再继续。
   - 不要把无关重构和功能开发打包在一起。

4. 警惕静默失败。
   - 接入真实 API 后移除演示 fallback。
   - 清晰暴露后端错误。
   - 验证数据是否真实落库，而不是只更新了组件本地状态。

5. 偏好朴素、可读的代码。
   - 清晰命名和直接控制流优于聪明技巧。
   - 在真正的共享抽象显现前，适度重复可以接受。
   - 注释解释非显而易见的决策，不复述代码本身。

6. 尊重数据。
   - 迁移必须保留已有数据。
   - 清理 smoke test 创建的测试数据。
   - 查询数据库前，不要凭空编造 ID、权限或资产范围。

7. 完成前验证。
   - 代码能编译不代表功能完成。
   - UI/API 工作在可行时应验证真实接口和渲染页面。
   - 如果验证不完整，明确说明已检查和未检查的部分。

## 当前项目特有注意事项

- Java 必须使用 17。
- `JWT_SECRET` 至少 32 bytes。
- 后端运行依赖 MySQL 和 Redis。
- 如果种子权限或角色分配不正确，API 权限过滤可能阻断管理接口。
- 访问 Token 的资产范围必须是真实且当前用户可读的数据资产 ID。
- CSV 导入和动态表查询同时依赖元数据表和物理动态表。
- 工作区可能已有其他开发中的未提交改动；编辑前先检查，不要覆盖。
- 只用中文沟通，编写文档。
- 测试通过后，交付时前端界面必须对接后端真实的接口，不允许mock。
