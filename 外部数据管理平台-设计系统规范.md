# 外部数据管理平台 — UI 设计系统规范

所有后续 UI 开发必须遵循本设计系统。本规范优先级高于任何默认设计行为。

---

## 一、色彩体系

所有颜色均以 CSS 自定义属性形式定义于 `src/styles/theme.css`，通过 Tailwind 工具类使用。**禁止在组件中硬编码十六进制色值**，始终使用 Token 类名。

### 基础色彩 Token

| Token 名称 | 亮色值 | Tailwind 类名 |
|---|---|---|
| background | `#f8f9fb` | `bg-background` |
| foreground | `#1a1d2e` | `text-foreground` |
| card | `#ffffff` | `bg-card` |
| primary（主色）| `#3b82f6` | `bg-primary` / `text-primary` |
| primary-foreground | `#ffffff` | `text-primary-foreground` |
| secondary / muted | `#f1f5f9` | `bg-muted` / `bg-secondary` |
| muted-foreground | `#64748b` | `text-muted-foreground` |
| accent | `#e0e7ff` | `bg-accent` |
| accent-foreground | `#3b82f6` | `text-accent-foreground` |
| destructive（危险）| `#ef4444` | `bg-destructive` / `text-destructive` |
| border | `#e2e8f0` | `border-border` |
| success（成功）| `#10b981` | `text-green-600` / `bg-green-50` |
| warning（警告）| `#f59e0b` | `text-amber-700` / `bg-amber-50` |

### 语义色彩模式（图标背景、徽标）

| 语义 | 背景类 | 文字类 |
|---|---|---|
| 主要 / 数据 | `bg-blue-50` | `text-blue-600` |
| 成功 / 激活 | `bg-green-50` | `text-green-600` |
| 警告 / 所有者 | `bg-amber-50` | `text-amber-700` |
| 统计 / 数量 | `bg-purple-50` | `text-purple-600` |
| 危险操作 | `bg-destructive` | `text-destructive-foreground` |

### 图表调色板（按顺序使用）

`#3b82f6`（蓝）→ `#10b981`（绿）→ `#f59e0b`（琥珀）→ `#8b5cf6`（紫）→ `#ec4899`（粉）

最多使用 5 色，不得超出此顺序范围。

---

## 二、字体排版

- **基准字号**：14px（`--font-size: 14px`）
- **字体族**：系统默认字体栈，不引入自定义字体
- **字重**：400（正文）、500 / 600（标题、标签、按钮）
- **行高**：所有层级统一为 1.5

### 字号规范

| 元素 | Tailwind 类 | 字重 |
|---|---|---|
| 页面主标题（h1）| `text-2xl` | `font-semibold` |
| 区块标题（h2）| `text-xl` | `font-semibold` |
| 卡片标题 | `text-base` | `font-semibold` |
| 正文 / 表格单元格 | `text-sm` | `font-normal` |
| 辅助说明 / 时间戳 | `text-xs` | `font-normal` / `font-medium` |
| 侧边栏导航标签 | `text-sm` | `font-normal` |

---

## 三、间距与布局

- **页面内边距**：`p-6`
- **标题下方间距**：`mb-6`
- **卡片网格间距**：`gap-4`
- **表单字段间距**：`space-y-4`
- **图标与文字间距**：`gap-2`（侧边栏导航用 `gap-3`）
- **顶部导航栏高度**：`h-14`

### 网格列数规范

| 场景 | 列数配置 |
|---|---|
| 统计卡片行 | `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` |
| 资产卡片网格 | `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` |
| 全宽数据表格 | 单列，`w-full` |

---

## 四、圆角规范

| 类名 | 数值 | 适用场景 |
|---|---|---|
| `rounded-sm` | 4px | 权限标签、小徽标 |
| `rounded-md` | 6px | 标签页、小按钮 |
| `rounded-lg` | 8px（`--radius`）| 卡片、弹窗、输入框 |
| `rounded-xl` | 12px | Logo 图标容器、大型图标块 |

---

## 五、阴影与层叠

- **默认卡片**：`border shadow-sm`
- **可交互卡片（悬停）**：`hover:shadow-md transition-all`
- **登录页卡片**：`border shadow-xl bg-card/95 backdrop-blur`
- **侧边栏 / 顶栏**：不使用阴影，改用 `border-r` / `border-b` 分隔

---

## 六、页面整体布局

系统采用**左侧固定侧边栏 + 右侧可滚动内容区**的两栏结构。

| 区域 | 背景 | 说明 |
|---|---|---|
| 侧边栏 | `bg-card`（白色）| 含 `border-r` 分隔线 |
| 顶部导航栏 | `bg-card`（白色）| 含 `border-b` 分隔线，高度 `h-14`，粘性定位 |
| 内容区 | `bg-background`（`#f8f9fb`）| 可滚动，内边距 `p-6` |

---

## 七、组件规范

### Logo 图标块

```jsx
<div className="size-8 rounded-lg bg-primary flex items-center justify-center">
  <Database className="size-4 text-primary-foreground" />
</div>
```

### 侧边栏导航项

- **未激活**：`hover:bg-sidebar-accent text-foreground`
- **激活状态**：`data-[active=true]:bg-primary data-[active=true]:text-primary-foreground`
- 图标尺寸 `size-4`，标签 `text-sm`

### 顶部标签栏（多页签）

- **当前页签**：`bg-primary/10 text-primary`
- **非当前页签**：`hover:bg-muted text-muted-foreground hover:text-foreground`
- 关闭按钮：悬停时或当前页签时显示

### 卡片

```jsx
<Card className="border shadow-sm hover:shadow-md transition-all cursor-pointer group">
```

- 图标容器：`p-2 bg-blue-50 rounded-lg group-hover:bg-blue-100 transition-colors`
- 卡片标题：`text-base font-semibold text-foreground`
- 卡片描述：`text-xs leading-relaxed text-muted-foreground`
- 内部分隔线：`border-t` + `py-2`

### 统计卡片

结构固定：**着色图标块** → **大数字**（`text-2xl font-bold`）→ **趋势行**（`text-xs`，绿色 `TrendingUp` 图标）

### 按钮

| 变体 | 使用场景 |
|---|---|
| 默认（实心主色）| 主要操作：提交、确认 |
| `outline` | 次要操作：取消、SSO 登录 |
| `ghost` | 工具栏图标按钮（铃铛、用户头像）|
| `destructive` | 删除等危险操作 |

- 表单内全宽按钮：`h-11 w-full`
- 工具栏图标按钮：`size="icon"`（ghost 变体）

### 输入框

- 高度：`h-11`
- 带前缀图标：`pl-10`，图标绝对定位 `left-3 top-1/2 -translate-y-1/2 size-4 text-muted-foreground`

### 徽标与权限标签

- 状态徽标：`<Badge variant="outline">` + 着色背景 + 对应文字色
- 所有者标签：`bg-amber-50 text-amber-700 border-amber-200`
- 权限小标签：`px-2 py-0.5 text-xs bg-blue-50 text-blue-700 rounded font-medium`

### 数据表格

- 使用 Shadcn `<Table>` 组件族
- 表头：`text-xs font-medium text-muted-foreground uppercase`
- 行悬停：`hover:bg-muted/50`
- 操作列：`<DropdownMenu>` + `MoreVertical` 触发图标

### 弹窗 / 对话框

- 使用 Shadcn `<Dialog>` 组件族
- 表单布局：`space-y-4`，`<Label>` + `<Input>` 配对
- 底部操作：`<DialogFooter>` 含取消（outline）+ 确认（default）按钮

### 标签页

- 使用 Shadcn `<Tabs>` / `<TabsList>` / `<TabsTrigger>`
- 带图标的触发器：`className="gap-2"` + `<Icon className="size-4" />`

---

## 八、图标规范

**统一使用 lucide-react**，按需单独导入，禁止使用其他图标库。

| 尺寸 | 使用场景 |
|---|---|
| `size-3` | 标签页关闭按钮 |
| `size-3.5` | 标签页内图标 |
| `size-4` | 侧边栏导航、按钮图标、表单前缀图标 |
| `size-5` | 顶部工具栏图标 |
| `size-8` | 登录页 Logo 图标 |

---

## 九、标准页面骨架

每个主功能页面遵循以下结构：

```tsx
<div className="flex-1 overflow-auto bg-background">
  <div className="p-6">
    {/* 页面头部 */}
    <div className="mb-6">
      <h1 className="text-2xl font-semibold text-foreground mb-1">页面标题</h1>
      <p className="text-sm text-muted-foreground">副标题或功能说明</p>
    </div>

    {/* 统计卡片行（可选）*/}
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 mb-6">
      {/* 统计卡片 */}
    </div>

    {/* 主内容区（表格或卡片网格，可用 Tabs 包裹）*/}
  </div>
</div>
```

---

## 十、交互与动效

- **颜色过渡**：`transition-colors`
- **阴影 + 颜色综合过渡**：`transition-all`
- **页面加载**：无入场动画
- **弹窗开关**：使用 Radix UI 内置过渡，无需额外配置

---

## 十一、登录页规范

- 全屏居中布局：`min-h-screen flex items-center justify-center`
- 背景渐变：`bg-gradient-to-br from-blue-50 via-slate-50 to-purple-50`
- 半透明遮罩层：`bg-gradient-to-br from-blue-100/40 via-white/60 to-purple-100/40`
- 卡片宽度：`max-w-md`，样式：`shadow-xl bg-card/95 backdrop-blur`
- Logo 容器：`size-16 bg-primary rounded-xl shadow-lg`
- 主标题：`text-3xl font-bold`
- 副标题：`text-sm text-muted-foreground`

---

## 十二、规范检查清单

### ✅ 应当这样做

- 所有颜色使用 Token 类（`bg-primary`、`text-muted-foreground`）
- 所有卡片加 `border shadow-sm`，可交互卡片加 `hover:shadow-md`
- 页面内边距保持 `p-6`
- 辅助文字、时间戳、元信息使用 `text-xs`
- 网格间距统一使用 `gap-4`
- 图标统一使用 lucide-react

### ❌ 禁止这样做

- 禁止硬编码十六进制色值或使用内联 style 设置颜色
- 禁止在内部页面中使用超过 `text-2xl` 的字号
- 禁止给侧边栏和顶栏加阴影（用边框线代替）
- 禁止对卡片使用 `rounded-full`，统一用 `rounded-lg`
- 禁止超出图表调色板顺序或使用超过 5 种图表色
